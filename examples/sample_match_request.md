# Sample Match Request

```text
SportMira, 看一下 Korea vs Czechia。盘口如果能识别就一起分析，我这场最多 30u，只买 2-3 种，默认保守。
```

Equivalent CLI:

```bash
python -m sportmira.cli analyze --match "Korea vs Czechia" --bankroll 30 --unit u --max-bets 3 --language zh
```
